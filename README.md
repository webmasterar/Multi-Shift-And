## Multi-Shift-And Algorithm

An implementation of the Multi-Shift-And algorithm for multiple exact pattern matching. Total length of patterns must be shorter than or equal to the computer word size (usually 64).
